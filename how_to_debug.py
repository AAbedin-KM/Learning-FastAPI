#to debug a code ebfore runnign so that when running no error occurs
#first make a breakpoint by clicking on the left of a line and so everythign before it is ran and therefore can be looked into to find bugs
#then do control shift p to get into the search menu and then type debug
#then go on the library that u are currently using (so in this case it would be fastapi)
#then u then look at the terminal for any errors 
#u can also look at the run and debug menu and this shows u all the variables being used and their current values
#this can be checked to see if soemthing is not happening and why it isnt happening if due to a condition not being met


